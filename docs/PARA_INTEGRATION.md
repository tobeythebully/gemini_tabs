# PARA 폴더 연동 기능 기획안

## 개요

Gemini Tabs에 로컬 PARA 시스템 폴더와 연동하는 기능을 추가하여, 프로젝트별 컨텍스트 자동 로드, 프롬프트 관리, 결과물 저장을 지원합니다.

---

## 기술적 가능성

- ✅ Electron은 Node.js 기반 → `fs` 모듈로 로컬 파일 접근 가능
- ✅ 파일 감시 (`fs.watch`)로 실시간 동기화 가능
- ✅ 마크다운 파일 읽기/쓰기 가능

---

## 예상 PARA 폴더 구조

```
~/Documents/PARA/
├── Projects/
│   ├── 1주1릴스_챌린지_2기/
│   │   ├── context.md          ← 프로젝트 배경 설명
│   │   ├── prompts/
│   │   │   ├── 모집글_작성.md    ← 프로젝트별 프롬프트 템플릿
│   │   │   └── 피드백_생성.md
│   │   └── outputs/             ← Gemini 결과물 저장 위치
│   └── 콘텐츠_마케팅/
├── Areas/
├── Resources/
│   └── prompts/                 ← 공용 프롬프트
│       ├── 요약.md
│       └── 번역.md
└── Archives/
```

---

## 사용자 플로우

### 1. 초기 설정
1. 설정(⚙️) → PARA 탭
2. PARA 루트 폴더 선택: `~/Documents/PARA/`
3. 저장 → 앱이 폴더 구조 스캔

### 2. 프로젝트 선택
1. 앱 상단에 프로젝트 선택 드롭다운 표시
2. 클릭 → Projects 폴더 내 프로젝트 목록 표시
3. 프로젝트 선택 시 `context.md` 자동 로드

### 3. 컨텍스트 자동 주입
- 프로젝트 선택 시 `context.md` 내용이 UI에 표시
- 새 대화 시작 시 자동으로 Gemini에 전달 (또는 `/context` 명령어로 수동)

### 4. 프로젝트별 프롬프트
- `/` 입력 시 드롭다운에 두 종류 표시:
  - 📁 현재 프로젝트 프롬프트 (`Projects/프로젝트명/prompts/`)
  - 📂 공용 프롬프트 (`Resources/prompts/`)

### 5. 결과물 저장
- Gemini 응답 옆에 `💾 저장` 버튼
- 클릭 시 파일명 입력
- 자동으로 `Projects/프로젝트명/outputs/` 에 마크다운 저장

---

## 구현 범위 (MVP)

### Phase 1: 기본 연동
- [ ] 설정에서 PARA 루트 폴더 지정
- [ ] Projects 폴더 스캔하여 프로젝트 목록 표시
- [ ] 프로젝트 선택 UI (드롭다운)

### Phase 2: 컨텍스트 로드
- [ ] 프로젝트 선택 시 `context.md` 읽기
- [ ] 컨텍스트 미리보기 UI
- [ ] `/context` 명령어로 채팅에 삽입

### Phase 3: 프롬프트 연동
- [ ] 프로젝트별 `prompts/` 폴더에서 프롬프트 로드
- [ ] 공용 `Resources/prompts/` 폴더에서 프롬프트 로드
- [ ] 슬래시 명령어에 통합

### Phase 4: 결과물 저장
- [ ] 저장 버튼 UI
- [ ] 파일명 입력 모달
- [ ] `outputs/` 폴더에 마크다운 저장

---

## 결정 필요사항

| 항목 | 옵션 A | 옵션 B |
|------|--------|--------|
| context.md 주입 | 자동 (새 대화 시) | 수동 (`/context`) |
| 프롬프트 우선순위 | 프로젝트 > 공용 | 공용 > 프로젝트 |
| 저장 형식 | 선택된 응답만 | 전체 대화 기록 |
| 폴더 감시 | 실시간 (`fs.watch`) | 앱 시작 시만 스캔 |

---

## 기술 참고

### Node.js 파일 시스템 예시
```javascript
const fs = require('fs');
const path = require('path');

// PARA 폴더 읽기
const paraRoot = '/Users/tobeythebully/Documents/PARA';
const projects = fs.readdirSync(path.join(paraRoot, 'Projects'));

// context.md 읽기
const context = fs.readFileSync(
  path.join(paraRoot, 'Projects', projectName, 'context.md'),
  'utf-8'
);

// 결과물 저장
fs.writeFileSync(
  path.join(paraRoot, 'Projects', projectName, 'outputs', filename),
  content
);
```

### Electron IPC 통신
```javascript
// main.js
ipcMain.handle('read-para-projects', async (event, paraRoot) => {
  return fs.readdirSync(path.join(paraRoot, 'Projects'));
});

// renderer.js
const projects = await ipcRenderer.invoke('read-para-projects', paraRoot);
```

---

## 변경 이력
- 2026-01-06: 초안 작성
